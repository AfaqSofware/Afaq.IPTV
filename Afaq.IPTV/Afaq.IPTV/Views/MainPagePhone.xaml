<?xml version="1.0" encoding="utf-8"?>

<TabbedPage xmlns="http://xamarin.com/schemas/2014/forms"
            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
            xmlns:views="clr-namespace:Afaq.IPTV.Views;assembly=Afaq.IPTV"
            xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
            prism:ViewModelLocator.AutowireViewModel="True"
            x:Class="Afaq.IPTV.Views.MainPagePhone"
            ItemsSource="{Binding ChannelLists}" Appearing="MainPagePhone_OnAppearing" >
  
  <TabbedPage.ItemTemplate>
    <DataTemplate>
      <ContentPage Title="{Binding Name}">
        <StackLayout>
          <Entry Placeholder="Search Channel" Text="{Binding SearchKey, Mode=TwoWay}" />
          <ListView x:Name="MyChannelList"  ItemSelected="MyChannelList_OnItemSelected" ItemsSource="{Binding Channels, Mode=TwoWay}"
                   ItemTapped="ChannelList_OnItemTapped"  SelectedItem="{Binding CurrentChannel, Mode=TwoWay}">
            <ListView.ItemTemplate>
              <DataTemplate>
                <ImageCell ImageSource="{Binding Logo}" Text="{Binding Name}"  />
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackLayout>
      </ContentPage>
    </DataTemplate>
  </TabbedPage.ItemTemplate>
  
</TabbedPage>